# .env.example
# Copy to .env.local and fill in values

# For production: point to your deployed Cloudflare Worker
# Leave empty to use the local mock server (default for dev)
VITE_WORKER_URL=

# Used by mock-server/index.js if USE_GODADDY=true
GODADDY_KEY=
GODADDY_SECRET=
